/* draw_config.h
*
* Configuration and/or settings for drawing
*/

// TODO: Look into perhaps making this initialize or read
// from some config file? Maybe? Idk but for now this works
typedef enum {
    Blue,
    Red,
    Green,
    ProfileCount
} ColorProfile;
